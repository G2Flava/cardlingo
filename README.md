# CardLingo v1.0.5 安全分段翻译版

核心修复：受保护的模板变量、代码、正则、URL、HTML 与自定义保护词不再替换成占位符发送给模型，而是在浏览器侧直接切离；仅普通自然语言片段发送给模型，翻译完成后按原位置拼回。长文本会自动按自然边界分块，降低 2 万字以上角色卡字段的失败概率。

GitHub Pages 更新：只需用本目录的 index.html 覆盖仓库根目录 index.html 并 Commit。
