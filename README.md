# CardLingo v1.1.5 外显界面修复版

修复 v1.1.4 中 bookView 缺少闭合标签，导致 scriptsView / detectView 被错误嵌套进角色书页面，出现“检测成功但列表完全不显示”的问题。

现在四个主页面（角色 / 角色书 / 脚本触发器 / 外显检测）为同级视图。外显检测完成后会显示可翻译词条列表、逐条勾选、全选、语言设置和批量翻译按钮，并同步写回原字段。
